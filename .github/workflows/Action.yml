name: Angular 15

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hello world!"
